{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main Pickle window. Deliberately minimal: the app talks to servers through Rust, so the web layer needs no network, filesystem, or shell access of its own.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
