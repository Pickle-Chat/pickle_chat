{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main Pickle window. Deliberately minimal: the app talks to servers through Rust, so the web layer needs no network, filesystem, or shell access of its own. Global shortcuts are registered from Rust, so the web layer is granted nothing beyond the plugin's default.",
  "windows": ["main"],
  "permissions": ["core:default", "global-shortcut:default"]
}
